# Varlock schema for project environment.
# Values here are non-secret project defaults and are intentionally committed.
#
# Skip Playwright browser downloads during dependency installation and scripts.
# @type=enum(0, 1) @sensitive=false
PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1
